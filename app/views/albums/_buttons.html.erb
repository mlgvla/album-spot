<!-- button logic for Album Show Page -->

<!-- if album is NOT in user's user_albums 
    Add to Collection button
    elseif there is NO review
        Add Review Button
        else
            Edit Review Button 
        end
    end -->

    <% if !current_user.user_albums.any? {|user_album| user_album.album.id == album.id} %> <!-- put this in a do/end with an if so user_album  -->
        Add to Collection button   
    <% else %>
        DOES A REVIEW EXIST? Maybe use an model instance method?
        <% if !current_user.user_albums.review %>
          Add Review Button
        <% else %>
          Edit Review Button
        <% end %>
    <% end %>